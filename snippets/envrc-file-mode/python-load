# -*- mode: snippet -*-
# name: python-load
# key: python-load
# --
export PATH=$HOME/.pyenv/bin:$PATH
eval "$(pyenv init -)"
layout pyenv ${1:$$(yas-choose-value (split-string (shell-command-to-string "~/.pyenv/bin/pyenv versions --bare") "\n" t))}